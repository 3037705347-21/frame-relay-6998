# 修复前故障复现（Docker）

## 项目与标准命令

项目是 Go 协议帧协调库，模块为 `example.com/frame-relay`。标准验证命令为：

```text
go test -count=20 ./...
```

## 环境构建与编译

在当前 `linux/amd64` 平台使用 `golang:1.26` 构建镜像成功。容器内执行 `go build ./...` 成功。

## 故障触发步骤

在容器内执行标准验证命令。

## 实际错误输出

```text
FAIL
```

## 期望行为

已取消的调用不能写入帧，也不能为该流留下任何可见状态。
